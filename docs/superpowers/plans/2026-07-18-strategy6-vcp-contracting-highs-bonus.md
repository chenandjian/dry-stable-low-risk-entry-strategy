# Strategy6 VCP Contracting Highs Bonus Plan

## 目标

为策略6主链VCP增加“高点不升且低点严格抬高”固定2分奖励，并保持旧接口、旧字段和评分上限兼容。

## 实施步骤

1. 在 `tests/test_strategy6_pattern.py` 增加高点持平/下降、某轮高点升高、低点持平/下降和轮次不足的失败测试。
2. 在 `strategy6/pattern.py` 增加纯证据判定函数，仅为符合条件的主链VCP追加原因标签。
3. 增加评分失败测试，证明该标签增加2分，并验证与已有低点奖励可同时触发且不突破15分上限。
4. 在 `strategy6/scorer.py` 消费新标签，记录可审计评分原因。
5. 运行策略6专项、全部策略6测试、Python编译和后端完整回归。
6. 审核无中高等级问题后，仅提交本次文件并推送当前分支。
