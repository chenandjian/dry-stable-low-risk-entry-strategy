# 批量评分复选取消选择验收

## 实现
- 修改 `web/src/pages/Strategy6BatchEvaluation.vue`：跌不动确认强度、EMA等级、K线集合形态等级的展开区各增加“取消选择”。
- 点击仅清空当前数组，恢复该组不限；保留其他组、数值范围与单选条件。不重新评分、不改后端和选股逻辑。
- 无勾选时禁用按钮，展开区保持打开；原有“清除筛选”仍可清空全部筛选。

## 测试与审核
- 先新增三组参数化测试，修改前因缺少按钮失败，修改后全部通过。
- `npm --prefix web test -- --run`：25个文件、171项通过。
- `npm --prefix web run build`：成功，仍有既有Vite弃用和大包警告。
- `git diff --check`（本次代码文件）：通过。
- 审核重点：组间隔离、保留最低分、禁用状态、按钮可见性、无额外接口请求。未发现中高等级问题。
- 未运行后端全量测试，因无后端改动；未做真实浏览器人工验收。
